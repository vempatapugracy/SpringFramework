Eager and Lazy Instantiation

By Default spring uses eager singleton instantiation means creates the opjects at application startup.

Creating object at the first access is lazy instantiation. @Lazy on top of class makes that bean lazy instantiation