Generally Identical Beans are added to context with names as method names

But to give custom names to take control, we can have 3 methods to give our own name
@Bean(name="-----")
@Bean(value="-----")
@Bean("-----")
