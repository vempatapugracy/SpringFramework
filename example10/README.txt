Wiring Beans though Dependency injection by passing parameter

As we use @Bean for a method to create a bean in spring context, passing dependency object as a parameter, then spring detect automatically this class is dependent on the object and inject already present bean type object into that parameter.
